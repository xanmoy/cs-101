#include<iostream>
using namespace std;
int main(){
    int a = 123;
    cout << a << endl;
    // char a = 'v';
    char b= 'v';
    cout << b << endl;
    bool bl = true;
    cout << bl << endl;
    float f = 10.5;
    cout << f << endl;
    double d = 10.5;
    cout << d << endl;

    int size = sizeof(a); // sizeof is a function which returns the size of the datatype in bytes
    cout << "size of a is " << size << endl;

    // How data is stored in memory
    //  1 byte = 8 bits (1 bit is either 0 or 1) 

    
}