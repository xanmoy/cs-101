let names = ["Tanmoy", "jack", "Jessica", "Tanmoy", "Tanmoy"]

// names.map((name) => {
//     console.log(name)
//     return (
//         name + "1"
//     )
    
// })
names.map((name) => {
    
    return (
        <h1>{name}</h1>
    )
    
})
name.filter((name) => {
    return name !== "Tanmoy"
})

